# 🚀 Настройка бота на Timeweb Cloud Platform

## ⚠️ ВАЖНО: Настройки в панели Timeweb

В панели управления Timeweb **НЕ нужно** указывать команды `docker build` и `docker run`!

Timeweb автоматически использует Dockerfile из вашего репозитория.

## 📋 Правильные настройки в панели Timeweb

### Основные настройки:

1. **Окружение (Environment):** `Node.js`
2. **Фреймворк (Framework):** `Nest` (или `Other`)
3. **Версия окружения (Environment Version):** `24`

### Команды (ОСТАВЬТЕ ПУСТЫМИ или используйте стандартные):

- **Команда сборки (Build Command):** Оставьте пустым или `npm ci`
- **Зависимости (Dependencies):** Оставьте пустым
- **Команда запуска (Run Command):** Оставьте пустым или `npm start`
- **Путь проверки состояния (Health Check Path):** Оставьте пустым

### Переменные окружения:

Добавьте в разделе "Переменные окружения" (Environment Variables):

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_api_ключ
KIE_API_URL=https://api.kie.ai
KIE_TIMEOUT_SECONDS=30
ADMIN_ID=ваш_telegram_id
PAYMENT_CARD_HOLDER=Дмитрий
PAYMENT_PHONE=89962176524
PAYMENT_BANK=Юмани
SUPPORT_TELEGRAM=@username
SUPPORT_TEXT=Если у вас возникли вопросы, напишите администратору
```

## 🔧 Что делает Dockerfile

Dockerfile автоматически:
1. Устанавливает Node.js 24
2. Устанавливает Python 3 и pip
3. Устанавливает Node.js зависимости (`npm ci`)
4. Устанавливает Python зависимости (`pip install`)
5. Копирует все файлы
6. Запускает бота через `npm start`

## ✅ Проверка работы

После деплоя:
1. Проверьте логи в панели Timeweb
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить

## 🐛 Решение проблем

### Ошибка "docker: not found"
- **Проблема:** В настройках указаны команды `docker build` или `docker run`
- **Решение:** Удалите эти команды из настроек, оставьте пустыми

### Ошибка "Python not found"
- **Проблема:** Python не установлен в контейнере
- **Решение:** Убедитесь что Dockerfile содержит установку Python 3

### Ошибка "Module not found"
- **Проблема:** Зависимости не установлены
- **Решение:** Проверьте что `package.json` и `requirements.txt` на месте

### Бот не запускается
- **Проблема:** Не установлены переменные окружения
- **Решение:** Добавьте все необходимые переменные в панели Timeweb

## 📝 Структура файлов

Убедитесь что в репозитории есть:
- ✅ `Dockerfile` - для сборки образа
- ✅ `package.json` - зависимости Node.js
- ✅ `requirements.txt` - зависимости Python
- ✅ `index.js` - точка входа (запускает Python бота)
- ✅ `run_bot.py` - основной Python скрипт
- ✅ `.dockerignore` - исключает ненужные файлы

## 🔄 Обновление бота

1. Загрузите новые файлы в репозиторий
2. Timeweb автоматически пересоберет образ
3. Бот перезапустится с новыми изменениями

---

**Главное правило:** В настройках Timeweb НЕ указывайте команды docker build/run - они выполняются автоматически через Dockerfile!

